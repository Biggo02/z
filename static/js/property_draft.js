(function(){
  'use strict';
  const form=document.querySelector('form[enctype="multipart/form-data"]');
  if(!form || !document.getElementById('rooms-area')) return;
  const pathMatch=location.pathname.match(/\/properties\/([^/]+)\/edit\/?$/);
  const serverPropertyId=pathMatch?decodeURIComponent(pathMatch[1]):'';
  const draftKey='fasthome-property-draft-v3:'+location.pathname;
  const saveUrl='/properties/autosave/';
  const headings=[...form.querySelectorAll('h2')];
  const steps=[{title:'Logement',icon:'🏠',match:'Informations du logement'},{title:'Services',icon:'💧',match:'Eau et'},{title:'Photos',icon:'📷',match:'Photos du logement'},{title:'Localisation',icon:'📍',match:'Localisation'},{title:'Publication',icon:'📢',match:'Extérieur, équipements et sécurité'}];
  const style=document.createElement('style');style.textContent=`.draft-progress{margin:0 0 18px;padding:12px;background:#f7f9fb;border:1px solid #e4e9ee;border-radius:15px;position:sticky;top:8px;z-index:20}.draft-progress-top{display:flex;align-items:center;justify-content:space-between;gap:8px;margin-bottom:10px}.draft-progress-title{font-weight:800;color:#18344d}.draft-save-state{font-size:.72rem;color:#64707d}.draft-bar{height:7px;background:#e4e9ee;border-radius:99px;overflow:hidden}.draft-bar>span{display:block;height:100%;width:0;background:#163a5f;border-radius:inherit;transition:width .25s ease}.draft-steps{display:grid;grid-template-columns:repeat(5,1fr);gap:5px;margin-top:10px}.draft-step{border:0;background:transparent;padding:5px 2px;border-radius:9px;cursor:pointer;color:#65717d;font-size:.65rem;font-weight:700}.draft-step.active{background:#e7eef5;color:#163a5f}.draft-step.done{color:#24613f}.draft-step-icon{display:block;font-size:1rem;margin-bottom:2px}.draft-panel{display:none}.draft-panel.active{display:block}.draft-nav{display:flex;justify-content:space-between;gap:8px;margin:14px 0 4px}.draft-nav .btn{min-width:120px}.draft-resume{padding:11px 12px;background:#edf5ff;border:1px solid #cddff1;border-radius:10px;margin-bottom:12px;font-size:.78rem}.draft-resume button{margin-left:8px;border:0;background:none;color:#163a5f;font-weight:800;cursor:pointer}@media(max-width:760px){.draft-progress{top:4px;padding:9px}.draft-steps{gap:2px}.draft-step{font-size:.57rem}.draft-step-icon{font-size:.9rem}.draft-nav .btn{min-width:0;flex:1;font-size:.75rem;padding:9px 7px}}`;document.head.appendChild(style);
  const progress=document.createElement('div');progress.className='draft-progress';progress.innerHTML='<div class="draft-progress-top"><span class="draft-progress-title">Préparation de la publication</span><span class="draft-save-state" id="draftSaveState">Brouillon non enregistré</span></div><div class="draft-bar"><span id="draftBarFill"></span></div><div class="draft-steps"></div>';form.parentNode.insertBefore(progress,form);const stepRow=progress.querySelector('.draft-steps');
  const panels=[];headings.forEach((heading,i)=>{const panel=document.createElement('section');panel.className='draft-panel';const title=heading.textContent.trim();heading.parentNode.insertBefore(panel,heading);panel.appendChild(heading);let node=panel.nextSibling;while(node&&!(node.nodeType===1&&node.tagName==='H2')){const next=node.nextSibling;panel.appendChild(node);node=next}panels.push({panel,title,index:Math.min(i,4)})});
  const findPanel=t=>panels.find(p=>p.title.includes(t));const p1=findPanel('Informations du logement'),p2=findPanel('Eau et'),p3=findPanel('Photos du logement'),p4=findPanel('Localisation'),p5=findPanel('Extérieur, équipements et sécurité');const effectivePanels=[p1,p2,p3,p4,p5].filter(Boolean);if(p5){const fi=panels.indexOf(p5);for(let i=fi+1;i<panels.length;i++){const p=panels[i];while(p.panel.firstChild)p5.panel.appendChild(p.panel.firstChild);p.panel.remove()}}
  effectivePanels.forEach((p,i)=>{const s=steps[i]||steps[4];const b=document.createElement('button');b.type='button';b.className='draft-step';b.dataset.index=i;b.innerHTML=`<span class="draft-step-icon">${s.icon}</span>${s.title}`;b.onclick=()=>show(i);stepRow.appendChild(b);p.panel.classList.add('draft-panel')});
  let current=0,propertyId=serverPropertyId||form.dataset.propertyId||'',saving=false,submitting=false,timer=null;
  const fields=()=>[...form.querySelectorAll('input,select,textarea')].filter(el=>el.name&&el.type!=='file'&&el.name!=='csrfmiddlewaretoken'&&!el.disabled);
  const requiredCompletion=()=>{const f=fields(),r=f.filter(x=>x.required);return r.length?r.filter(x=>String(x.value||'').trim()).length/r.length:f.filter(x=>String(x.value||'').trim()).length/Math.max(f.length,1)};
  const state=t=>{const el=document.getElementById('draftSaveState');if(el)el.textContent=t};
  const csrf=()=>{const el=form.querySelector('[name=csrfmiddlewaretoken]');return el?el.value:''};
  const saveLocal=()=>{const d={};fields().forEach(el=>d[el.name]=el.type==='checkbox'?el.checked:el.value);try{localStorage.setItem(draftKey,JSON.stringify(d))}catch(e){}};
  const restoreLocal=()=>{let raw=null;try{raw=localStorage.getItem(draftKey)}catch(e){}if(!raw)return;let d;try{d=JSON.parse(raw)}catch(e){return}let changed=false;fields().forEach(el=>{if(!(el.name in d))return;if(el.type==='checkbox'){if(el.checked!==!!d[el.name]){el.checked=!!d[el.name];changed=true}}else if(!el.value&&d[el.name]){el.value=d[el.name];changed=true}});if(changed){const n=document.createElement('div');n.className='draft-resume';n.innerHTML='<strong>Votre saisie précédente a été retrouvée.</strong> <button type="button">Conserver</button> <button type="button">Effacer</button>';form.parentNode.insertBefore(n,form);n.querySelector('button').onclick=()=>n.remove();n.querySelectorAll('button')[1].onclick=()=>{try{localStorage.removeItem(draftKey)}catch(e){}location.reload()}}};
  const updateProgress=()=>{const pct=Math.round(requiredCompletion()*100);document.getElementById('draftBarFill').style.width=pct+'%';document.querySelectorAll('.draft-step').forEach((b,i)=>{b.classList.toggle('active',i===current);b.classList.toggle('done',i<current)})};
  function show(i){current=Math.max(0,Math.min(effectivePanels.length-1,i));effectivePanels.forEach((p,n)=>p.panel.classList.toggle('active',n===current));let nav=effectivePanels[current].panel.querySelector('.draft-nav');if(!nav){nav=document.createElement('div');nav.className='draft-nav';nav.innerHTML='<button type="button" class="btn btn-light draft-prev">← Précédent</button><button type="button" class="btn btn-primary draft-next">Continuer →</button>';effectivePanels[current].panel.appendChild(nav);nav.querySelector('.draft-prev').onclick=()=>show(current-1);nav.querySelector('.draft-next').onclick=()=>show(current+1)}nav.querySelector('.draft-prev').style.visibility=current?'visible':'hidden';nav.querySelector('.draft-next').textContent=current===effectivePanels.length-1?'Vérifier la publication →':'Continuer →';updateProgress();window.scrollTo({top:0,behavior:'smooth'})}
  async function saveServer(includeFiles){if(submitting||saving)return;const type=form.querySelector('[name="property_type"]');if(!propertyId&&(!type||!type.value)){state('Sélectionnez le type pour sauvegarder');saveLocal();return}saving=true;state('Sauvegarde…');try{const data=new FormData;fields().forEach(el=>{if(el.type==='checkbox'){if(el.checked)data.append(el.name,'on')}else data.append(el.name,el.value)});if(propertyId)data.append('property_id',propertyId);if(includeFiles)form.querySelectorAll('input[type=file][name]').forEach(input=>[...input.files].forEach(file=>data.append(input.name,file)));const r=await fetch(saveUrl,{method:'POST',headers:{'X-CSRFToken':csrf(),'X-Requested-With':'XMLHttpRequest'},body:data,credentials:'same-origin'}),p=await r.json().catch(()=>({}));if(!r.ok||!p.ok)throw new Error(p.error||'Impossible de sauvegarder le brouillon.');propertyId=p.property_id||propertyId;form.dataset.propertyId=propertyId;if(propertyId){const editUrl='/properties/'+encodeURIComponent(propertyId)+'/edit/';if(location.pathname!==editUrl)history.replaceState({propertyId},'',editUrl);try{localStorage.removeItem(draftKey)}catch(e){}}state('Brouillon sauvegardé automatiquement')}catch(e){state('Sauvegarde locale — '+(e.message||'serveur indisponible'));saveLocal()}finally{saving=false}}
  const queueSave=includeFiles=>{clearTimeout(timer);timer=setTimeout(()=>saveServer(includeFiles),includeFiles?100:900)};
  form.addEventListener('input',()=>{if(!submitting){saveLocal();queueSave(false);updateProgress()}});form.addEventListener('change',e=>{if(!submitting){saveLocal();queueSave(e.target&&e.target.type==='file');updateProgress()}});form.querySelectorAll('input[type=file][name]').forEach(i=>i.addEventListener('change',()=>queueSave(true)));form.addEventListener('submit',()=>{submitting=true;clearTimeout(timer);try{localStorage.removeItem(draftKey)}catch(e){}});window.addEventListener('beforeunload',()=>{if(!submitting)saveLocal()});restoreLocal();show(0);updateProgress();if(propertyId)state('Brouillon enregistré sur le serveur');else if(requiredCompletion()>0)queueSave(false);
})();
